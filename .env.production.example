# Generate a fresh password with: openssl rand -hex 32
DATABASE_PASSWORD=
# Exact public HTTPS origin, with no trailing slash.
APP_ORIGIN=
APP_IMAGE=solar-management:local
APP_PORT=3001
# Confirm the number of trusted private reverse-proxy hops before deployment.
TRUST_PROXY=1
